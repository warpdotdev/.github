# Factory connection test

This pull request verifies your Factory's GitHub connection.

This is a connection test, not real work. Run every agent you dispatch on a cheap, fast, open-weight model — pass model_id auto-open if it is available, or any equivalent — keep it to an acknowledgement, and close out the factory task: no triage, no spec, no code, no new pull request.

It was seeded automatically. Once the Factory picks it up, it is safe to
close this pull request and delete the branch.
